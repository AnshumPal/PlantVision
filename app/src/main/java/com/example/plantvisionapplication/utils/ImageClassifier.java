package com.example.plantvisionapplication.utils;

import android.graphics.Bitmap;

public class ImageClassifier {
    public static String classify(Bitmap image) {
        // Dummy logic — replace this with actual ML inference
        return "This is a Neem leaf.";
    }
}
